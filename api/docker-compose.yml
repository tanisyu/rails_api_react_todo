version: '3'

services:
  web:
    build: .
    ports:
      - '3000:3000'
    volumes: 
      - '.:/usr/src/app'
    environment: 
      - 'MYSQL_ROOT_PASSWORD=root'
    # tty + stdin = -it
    tty: true
    stdin_open: true
    # db作成後にコンテナ作成
    #depends_on: 
    #  - db
    # webからdbを使えるようにする
    #links: 
    #  - db